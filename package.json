{
  "name": "memory-plugin-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/opencode-plugin",
    "packages/codex-plugin"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run build -w mmcp-1file-core && npm run test --workspaces",
    "bundle:core": "node scripts/sync-bundled-core.js",
    "build:codex-runtime": "node scripts/build-codex-runtime.js",
    "verify:codex-runtime": "node scripts/verify-codex-runtime.js",
    "pack:dry-run": "npm run bundle:core && npm pack --workspace opencode-mmcp-1file --dry-run",
    "verify:packages": "npm run bundle:core && node scripts/verify-package-contents.js && npm run build:codex-runtime && npm run verify:codex-runtime"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
