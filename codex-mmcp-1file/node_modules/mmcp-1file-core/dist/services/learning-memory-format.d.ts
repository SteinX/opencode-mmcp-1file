import type { LearningRecord, LearningSummary } from "./learning-memory-client.js";
export interface LearningMemoryFormatterConfig {
    includeEvidence: boolean;
    includeCandidates: boolean;
}
export interface LearningMemoryFormatInput {
    records: LearningRecord[];
    learning_summary?: LearningSummary;
}
export declare function formatLearningMemoryInjection(input: LearningMemoryFormatInput, config: LearningMemoryFormatterConfig): string | null;
