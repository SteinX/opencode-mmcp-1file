/**
 * Central connection state management for the MCP server.
 *
 * Tracks whether the server is reachable and manages a background retry loop
 * that periodically attempts reconnection when the connection has failed.
 *
 * Separated from mcp-client.ts to avoid circular dependencies — multiple
 * modules (tool-registry, system-prompt, index) need to read connection state.
 */
export declare function isConnectionFailed(key?: string): boolean;
export declare function markConnectionFailed(key?: string): void;
export declare function markConnectionHealthy(key?: string): void;
export interface ConnectionStatus {
    connected: boolean;
    failureCount: number;
    lastFailureTime: number | null;
    retrying: boolean;
}
export declare function getConnectionStatus(key?: string): ConnectionStatus;
/**
 * Start a periodic retry loop. The loop calls `retryFn` at `intervalMs`
 * intervals. When `retryFn` resolves to `true`, the loop stops and
 * `markConnectionHealthy()` is called automatically.
 */
export declare function startRetryLoop(retryFn: () => Promise<boolean>, intervalMs?: number, reconnectCallback?: () => void, key?: string): void;
export declare function stopRetryLoop(key?: string): void;
export declare function _resetForTest(): void;
