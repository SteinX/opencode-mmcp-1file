import type { PluginConfig } from "../config.js";
export declare function buildCompactionRecoveryContext(config: PluginConfig, compactSummary?: string): Promise<{
    text: string;
    count: number;
    skippedSimilarToSummary: number;
} | null>;
