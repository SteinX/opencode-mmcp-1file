import type { PluginConfig } from "../config.js";
interface SessionMessages {
    info: {
        id: string;
        role: string;
    };
    parts: Array<{
        type: string;
        text?: string;
    }>;
}
export declare function getLastCapturedId(sessionID: string): string | undefined;
export declare function performAutoCapture(config: PluginConfig, sessionID: string, messages: SessionMessages[], callLLM: (prompt: string) => Promise<string>): Promise<boolean>;
export {};
