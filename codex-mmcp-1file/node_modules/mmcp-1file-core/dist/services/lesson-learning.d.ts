import type { PluginConfig } from "../config.js";
import type { LearningMemoryCandidate } from "./preference-learning.js";
export type LessonKind = "project_lesson" | "project_pattern" | "project_pitfall";
interface ChatMessage {
    role: "system" | "user" | "assistant";
    content: string;
}
type LlmCaller = (config: PluginConfig, messages: ChatMessage[]) => Promise<string>;
export declare function buildLessonExtractionPrompt(text: string): ChatMessage[];
export declare function parseLessonCandidates(rawResponse: string, options: {
    minConfidence: number;
    minImportance: number;
}): LearningMemoryCandidate[];
export declare function extractLessonCandidates(config: PluginConfig, text: string, llmCaller?: LlmCaller): Promise<LearningMemoryCandidate[]>;
export {};
