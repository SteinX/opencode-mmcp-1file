import type { PluginConfig } from "../config.js";
interface ChatMessage {
    role: "system" | "user" | "assistant";
    content: string;
}
/**
 * Calls an OpenAI-compatible chat completions API.
 * Used exclusively for auto-capture summarization — keeps it out of the user's session.
 */
export declare function callChatCompletion(config: PluginConfig, messages: ChatMessage[]): Promise<string>;
export declare function summarizeExchange(config: PluginConfig, prompt: string): Promise<string>;
export {};
