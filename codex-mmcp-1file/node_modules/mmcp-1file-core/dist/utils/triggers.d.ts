/**
 * Smart trigger detection utilities for prompting agent to use memory tools.
 */
export interface TriggerResult {
    triggered: boolean;
    type: "decision" | "new_task" | "error_context" | "code_exploration" | null;
    message: string;
}
/**
 * Check all triggers and return the highest priority nudge.
 * Priority: decision > new_task > error_context > code_exploration
 */
export declare function checkTriggers(sessionID: string, agentText: string, userText: string): TriggerResult;
/**
 * Clear nudge history for a session (e.g., after compaction).
 */
export declare function clearNudgeHistory(sessionID: string): void;
