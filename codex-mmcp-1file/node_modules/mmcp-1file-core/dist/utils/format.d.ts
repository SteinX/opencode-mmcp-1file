import type { TierConfig } from "../config.js";
import type { KGCommunity } from "../services/mcp-client.js";
export interface MemoryEntry {
    id: string;
    content: string;
    memory_type?: string;
    score?: number;
    created_at?: string;
}
/**
 * Format recalled memories as a markdown block for injection into the conversation.
 */
export declare function formatMemoriesForInjection(memories: MemoryEntry[]): string;
/**
 * Format project knowledge memories for full injection (not query-dependent).
 * These are the most recent valid memories, injected regardless of semantic match.
 */
export declare function formatProjectKnowledge(memories: MemoryEntry[]): string;
/**
 * Format memories for compaction recovery — includes more context.
 */
export declare function formatMemoriesForRecovery(taskMemories: MemoryEntry[], contextMemories: MemoryEntry[]): string;
export declare function formatTieredProjectKnowledge(allocated: Map<number, MemoryEntry[]>, tiers: TierConfig[]): string;
export declare function formatKnowledgeGraph(communities: KGCommunity[], maxItems: number): string;
