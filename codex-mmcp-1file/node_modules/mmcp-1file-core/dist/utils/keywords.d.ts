export declare function detectMemoryKeyword(text: string, extraPatterns?: RegExp[]): string | null;
export declare const MEMORY_NUDGE_MESSAGE = "[MEMORY HINT] The user explicitly asked you to remember or save something. Use `memory_save` to persist the key fact for future sessions. Store explicit user-requested memories with the `USER:` prefix so they stay prioritized in project knowledge. Use other prefixes only when they fit better (DECISION:, TASK:, PATTERN:, BUGFIX:, CONTEXT:, RESEARCH:).";
